/* Program to convert Celcius to Fahrenheit */

#include <stdio.h>

main()
{
	float c, f;
	printf ("Enter the temperature in Celcius\n");
	scanf ("%f",&c);
	f = c * 9 / 5 + 32;
	printf ("%f",f);
	getch();
}

