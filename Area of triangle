#include <stdio.h>
#include <math.h>

main()
{
	float b, h, area;
	printf ("Enter the base value\n");
	scanf ("%f",&b);
	printf ("Enter the height value\n");
	scanf ("%f",&h);
	area = h *b / 2;
	printf ("%.2f",area);
	getch();
}

